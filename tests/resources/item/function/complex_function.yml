items:
  - type: function
    name: processData
    description: "Process an array of data with optional configuration."
    parameters:
      - name: data
        type: array
      - name: config
        type: array
        default: []
      - name: callback
        type: callable
        optional: true
        default: null
    return:
      type: array
